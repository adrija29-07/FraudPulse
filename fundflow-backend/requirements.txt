# ============================================================
# requirements.txt
# Save this at:  fundflow-backend/requirements.txt
# Install with:  pip install -r requirements.txt
# ============================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database — SQLite (built-in, no server needed)
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Anthropic AI
anthropic==0.40.0

# Graph analytics
networkx==3.4.2

# Utilities
pydantic==2.10.0
python-dotenv==1.0.1
httpx==0.28.0

# Optional — only needed if you install PostgreSQL later
# asyncpg==0.30.0

# Optional — only needed if you install Redis later
# redis[asyncio]==5.2.0

# Optional — only needed if you install Kafka later
# aiokafka==0.11.0